# repos/

**Git repositories**

This directory is reserved for future Git repository mirrors or clones if the studio needs to manage multiple projects or working copies.

Currently, the main repo is the parent directory with `.git/` at the root.
