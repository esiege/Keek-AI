# secrets/

**Credentials and environment files**

This directory contains:
- `.env` files with API keys and tokens
- Encrypted credentials
- Database connection strings
- Authentication secrets

**Security:** This directory should be added to `.gitignore` and never committed. Access is restricted.
